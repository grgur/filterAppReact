# filterAppReact
A small React demo

### Development

To run the application, do the following steps:

* install all the dependencies
```sh
$ npm install
```
* run the webpack dev server
```sh
$ npm start
```
